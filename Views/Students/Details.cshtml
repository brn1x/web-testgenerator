@model GeradorDeProvas.Models.Entity.PeriodStudent

@{
    ViewData["Title"] = "Student Details";
}

<h1>Student - Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Student
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Student.Name)
        </dd>
        <dt class="col-sm-2">
            Period
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Period.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Student.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
