@model GeradorDeProvas.Models.Entity.PeriodSubject

@{
    ViewData["Title"] = "Subject Details";
}

<h1>Subject - Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Subject
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Subject.Name)
        </dd>
        <dt class="col-sm-2">
            Period
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Period.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Subject.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
